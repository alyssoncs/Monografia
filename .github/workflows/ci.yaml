name: CI

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  build:
    name: build 🛠️ 
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - uses: actions/checkout@v4
      - name: Make PDF
        run: make
      - name: Upload pdf
        uses: actions/upload-artifact@v4
        with:
          name: monografia
          path: build/monografia.pdf

  deploy:
    name: deploy 🚀
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download pdf
        uses: actions/download-artifact@v4
        with:
          name: monografia
      - name: Move pdf to directory
        run: |
          mkdir out
          mv monografia.pdf out
      - name: Upload pdf to orphan branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: out
          branch: pdf



