name: CI

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  make-pdf:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - uses: actions/checkout@v4
      - name: Make PDF
        run: make

